type Test = record { id : principal; item : blob };
service : () -> {
  getAllImages : () -> (vec Test) query;
  getImage : (principal) -> (opt Test) query;
  greet : (text) -> (text) query;
  loginmethod : (text) -> (text);
  registermethod : (text) -> (text);
  retrieve_profile : (text) -> (text) query;
  sample : () -> (text) query;
  upload : (blob) -> (Test);
}
